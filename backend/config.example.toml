# Sabakan System Configuration

[server]
host = "0.0.0.0"
port = 1323

[database]
path = "./sabakan.db"

[logging]
level = "info"    # debug, info, warn, error
format = "text"   # json, text

[podman]
# Path to Podman socket
# For rootful: unix:///run/podman/podman.sock
# For rootless: unix://$XDG_RUNTIME_DIR/podman/podman.sock
# For remote: tcp://host:port or http://host:port
socket_path = "unix:///run/podman/podman.sock"

[jwt]
# IMPORTANT: Change this secret in production!
secret = "change-this-secret-in-production-32bytes!"
access_token_expiry = 15    # minutes
refresh_token_expiry = 7    # days

[redis]
# Redis connection URL
# Set to empty string to disable Redis (sessions won't persist across restarts)
url = "redis://localhost:6379"

[auth]
# Allow new user registration
allow_registration = true

[oauth.google]
# Google OAuth credentials (get from Google Cloud Console)
client_id = ""
client_secret = ""
redirect_url = "http://localhost:1323/auth/oauth/google/callback"

[oauth.discord]
# Discord OAuth credentials (get from Discord Developer Portal)
client_id = ""
client_secret = ""
redirect_url = "http://localhost:1323/auth/oauth/discord/callback"


